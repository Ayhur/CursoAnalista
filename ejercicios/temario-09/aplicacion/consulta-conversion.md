# Consulta de conversión

Dispones de `eventos(usuario_id, evento, fecha)` con eventos `registro` y `compra`.

1. Define el grano y el periodo de una consulta de conversión semanal.
2. Explica por qué contar filas de eventos no equivale a contar usuarios convertidos.
3. Describe una validación que harías si una AI te propone la consulta SQL o un pipeline de MongoDB.
